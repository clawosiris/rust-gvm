# 2026-03-20 — Release Orchestration, Supply Chain Hardening, Team Expansion

## Team Changes
- Added @y0urself (Jaspar) as admin — maintainer of greenbone/actions/release & pontos
- Added @irisblankenshipgb (Iris) as admin
- Added @NilsGeese with read permissions
- Added @henning as admin
- Added @daniel-grnbn upgraded to admin
- Added @js-gb with write (developer) permissions

## Supply Chain Security
- **All 3rd-party GitHub Actions pinned to commit SHAs** across all workflows (ci, nightly, release, security)
- Rule documented in `clawosiris/forge` → `docs/rules/supply-chain-security.md`

## Release Orchestration
- New repo: `clawosiris/release-orchestrator` — centralized release pipeline
- `release-orchestrated.yml` added — accepts version input from orchestrator
- Uses `pontos-release` CLI (via `greenbone/actions/setup-pontos`) for version management
- Handles Cargo.toml version bump + Cargo.lock regeneration + tag push
- `release.yml` triggers automatically from the pushed tag (requires `RELEASE_TOKEN`)
- Dependency order: rust-gvm → (rust-gvm-api + gvm-rools) → openvas-mcp-server

### Test Releases
- v0.2.1-alpha.1 through v0.2.1-alpha.4: iterating on GITHUB_TOKEN vs RELEASE_TOKEN, Cargo.lock sync
- v0.2.1-alpha5: pontos compact format test (pontos didn't accept dots)
- v0.2.1-alpha.6: ✅ first successful end-to-end orchestrated release with dot-separated format

### Pontos Contribution
- PR greenbone/pontos#1196: accept dot-separated pre-release identifiers in SemVer scheme
- Fix: `_PRE_RELEASE_REGEXP` updated to allow optional `.` between name and number
- Released in pontos v26.3.1 — unblocks dot-separated versions like `0.2.1-alpha.6`

## Process Rules Adopted
- All changes via PRs from feature branches (no direct push to main)
- Auto-merge (squash) on PRs — but **only with branch protection** (auto-merge workflow reverted after it merged without review)
- Draft PRs for work-in-progress
- Greenbone conventional commits for pontos contributions (`Add:`, `Fix:`, etc.)

## Closed PRs
- **#27** (ci: publish gvm-mock-server GHCR image) — closed as obsolete; GHCR now handled by release.yml via orchestrator

## Releases
- v0.2.1-alpha.1 — manual tag push (pre-orchestrator)
- v0.2.1-alpha.4 — first working release with RELEASE_TOKEN fix
- v0.2.1-alpha.6 — ✅ first fully orchestrated release with pontos

## CI Changes
- `release-orchestrated.yml` added (pontos-release + Cargo.lock regen)
- `auto-merge.yml` added then reverted (needs branch protection first)
- All workflows: 3rd-party actions pinned to SHAs
