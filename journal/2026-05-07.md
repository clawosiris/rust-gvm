# 2026-05-07 — Mainline Caught Up on Backlog, New Typed Scanner Helper Work Waiting

## Mainline & Merged Work

### `main` finally moved again after the 2026-05-04 journal baseline
- `main` picked up nine commits after the last journal entry, led by merged PRs #109 (`fix(ci): restore release.yml with SBOM, Docker, binaries`), #121 (`ci(deps): bump the actions group across 1 directory with 4 updates`), and #126 (`build(deps): bump rand from 0.8.5 to 0.8.6`) on 2026-05-04
- The repo then cleared a chunk of the journal backlog on 2026-05-05 by merging PRs #135, #136, #137, #138, and #139 for the 2026-04-30 through 2026-05-04 entries
- PR #145 (`ci: run security workflow on docs-only pull requests`) also landed on 2026-05-05, which materially improves branch-protection signal quality for future journal-only changes

## Pull Request Queue

### Fresh feature work replaced the old repair branch as the main thing to review
- Issue #143 (`feat(client): add missing typed scan-config and scanner helpers`) was opened on 2026-05-05 and already has PR #144 (`feat(client): complete typed scan config and scanner helpers`) open with fresh green `CI` and `Security` runs from 2026-05-06
- PR #141 (`build(deps): bump the cargo group across 1 directory with 7 updates`) is still open but is now `BEHIND`, so it has drifted from easy-merge maintenance into queue-management work
- Journal automation resumed immediately after the merge burst: PR #142 for 2026-05-05 is still open and approved, and PR #146 for 2026-05-06 is open with branch protection still requesting review

## Issues
- **Opened:** #143 (`feat(client): add missing typed scan-config and scanner helpers`)
- No issues were closed after the 2026-05-04 journal baseline
- The repo's active work is now concentrated in the new typed scan-config/scanner-helper lane rather than in the older CI-repair backlog

## CI Health
- PR #144 gives the repo its freshest feature signal with successful `CI` and `Security` pull-request runs on 2026-05-06
- `OpenSSF Scorecard` succeeded repeatedly on `main` on 2026-05-05 after the backlog merge wave
- The #145 workflow change is already paying off: journal PRs now receive `Security` coverage even when they are docs-only changes
