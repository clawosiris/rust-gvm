# 2026-08-09 — Typed Mutation Facade Opened and Remaining Consumer Contracts Were Scoped

## Typed Client Expansion
- PR [#460](https://github.com/greenbone-hive/rust-gvm/pull/460) opened against `devel` to expose the remaining credential, schedule, target, and task mutations through the high-level `GmpClient` facade.
- The change introduces explicit omit, set, and clear semantics for scalar entity relationships while keeping gvmd's `id="0"` detach representation inside the command builders. Mock-client and builder coverage exercise the new methods and detach shapes.

## Follow-up Scope
- Issue [#458](https://github.com/greenbone-hive/rust-gvm/issues/458) requests typed schedule input and observation for once, hourly, daily, weekly, and yearly recurrence, keeping iCalendar serialization and parsing out of consumers and preserving unsupported recurrence as an explicit state.
- Issue [#459](https://github.com/greenbone-hive/rust-gvm/issues/459) requests a typed observed credential kind so consumers can distinguish supported username/password credentials from SSH-key, SNMP, certificate, Kerberos, missing, and unknown kinds without parsing protocol strings.
- Issue [#457](https://github.com/greenbone-hive/rust-gvm/issues/457), which defined the typed mutation and scalar relationship requirements, is addressed by PR #460 but remains open pending review and merge.

## Validation State
- PR #460 is open, mergeable, and awaiting review. Its formatting, Rust test matrices, Python-GVM integration, coverage, documentation, dependency-policy, static-analysis, and security checks all pass; the downstream E2E trigger was skipped because the PR targets `devel`.
