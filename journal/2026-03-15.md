# 2026-03-15 — Supervisor Journal

## Human Directives
- Validate mock server by testing python-gvm against it (MCP server compatibility)
- Use Codex as much as possible for implementation
- Run Claude Code for code review
- Fix all review findings directly
- Add GitHub Actions for nightly builds and releases with cross-platform binaries
- Add README, docs, and implementation status page
- Update Dependabot config to best practices
- Fix all CI failures
- Create spec and issue for openvas-mcp-server integration tests
- Add @enderbee as collaborator

## Agent Actions (Chronological)

### MCP Server Compatibility Analysis
- Analyzed `clawosiris/openvas-mcp-server` repo: client architecture, connection styles (Unix socket / TLS), service layer GMP commands
- Traced python-gvm source: `determine_supported_gmp()` two-connection flow (connect → get_version → disconnect → reconnect → authenticate)
- Identified all GMP commands used by MCP server services (targets, tasks, reports, notes, overrides, tickets, assets, compliance, vulns)
- Created `spec/mcp-server-integration-spec.md` — gap analysis with 8 gaps identified

### Implementation (Codex-delegated)
- **Handler improvements:** `get_assets` (asset_type filtering), `get_results`, `get_nvts`, `create_note`/`create_override` (text+nvt_oid parsing), `create_ticket` (result_id+comment), `modify_ticket` (status), nested `get_report` XML rendering
- **CLI:** `--mode stateful` support added to standalone binary
- **Rust tests:** 5 new MCP compatibility tests (`stateful_mcp_compat.rs`)
- **Python integration test:** `tests/integration/test_python_gvm.py` — full lifecycle (15 steps: version negotiation, auth, target/task CRUD, notes CRUD, cleanup)
- **CI:** `Integration (python-gvm)` job added to ci.yml
- Fixed note/override `name` fallback for backward compatibility with existing tests

### Code Review (Claude Code)
- 17 findings identified (4 high, 4 medium, 9 low)
- High: broken `now_iso()`, broken `child_text()` parser, dead listener error branch, unescaped XML attributes
- Medium: incorrect singularization, silent seed() drop, unescaped error status_text, TOCTOU double-read
- Low: duplicated xml_escape, pub fields, linear search, static format!, doc comments, Python test improvements

### Review Fixes (Codex-delegated)
- Created `util.rs` — shared `now_iso()` (Rata Die algorithm), `xml_escape()`, `xml_escape_attr()`
- Fixed `parse_children()` to return `(Vec<ParsedElement>, Option<String>)` — `child_text()` now works
- Fixed `extract_attributes()` to use `attr.unescape_value()`
- Removed dead `XmlReader::feed()` error branch in listener
- Added `singularize_resource_type()` function replacing `trim_end_matches('s')`
- XML-escaped `status_text` in `error_response()`
- Eliminated TOCTOU in `handle_start_task`/`handle_resume_task`
- `ListenerState` fields → `pub(crate)`
- `KNOWN_COMMANDS` → `binary_search`
- `seed()` panics on non-Stateful mode
- Added 7 new unit tests for coverage gaps

### CI/CD Pipeline
- **Release workflow** (`.github/workflows/release.yml`): 5-target cross-platform builds (linux amd64/arm64/musl, macos amd64/arm64), tar.gz + sha256, GitHub Release with auto-generated notes
- **Nightly workflow** (`.github/workflows/nightly.yml`): daily 04:00 UTC + manual dispatch, full test suite + clippy strict + 5-target builds, rolling `nightly` pre-release
- Manually triggered nightly — verified all 5 binaries published (1.06–1.24 MB each)
- Fixed nightly release name (shell substitution → GITHUB_OUTPUT)

### CI Fixes (multiple iterations)
- `rustfmt.toml`: removed nightly-only options (wrap_comments, imports_granularity, etc.)
- `fault.rs`: added doc comments on enum variant struct fields
- `gvm-gmp/lib.rs`: fixed `[Request]` doc link
- `gvm-protocol/xml_reader.rs`: fixed `[feed]`/`[is_complete]` doc links
- `deny.toml`: migrated to cargo-deny v2 schema (removed deprecated advisories/licenses fields)
- Added `GPL-3.0-or-later` to allowed licenses
- Ignored RUSTSEC-2023-0071 (rsa Marvin Attack, transitive via russh) and RUSTSEC-2025-0134 (rustls-pemfile unmaintained)
- Set `wildcards = "allow"` for workspace-inherited deps

### Documentation
- `README.md`: project overview, crate structure table, quick start (library + binary), mock server features, architecture diagram, CI/CD table, pre-built binary downloads
- `docs/STATUS.md`: implementation status for all 5 crates, CRUD coverage matrix (19 resource types), feature tables, test breakdown (221 tests)

### Dependabot & PR Management
- `.github/dependabot.yml`: cargo (weekly, grouped minor/patch), github-actions (weekly, grouped), pip (monthly)
- Merged PR #11 (actions v4→v6), #12 (rstest 0.24→0.26), #14 (russh 0.48→0.57 security fix)
- Closed PR #10 (superseded), #13 (quick-xml breaking changes — deferred)

### Collaboration
- Added @enderbee as collaborator with write access

### openvas-mcp-server Integration
- Created `spec/mcp-server-mock-integration.md` — full test plan for MCP server integration tests using mock server
- Filed [clawosiris/openvas-mcp-server#31](https://github.com/clawosiris/openvas-mcp-server/issues/31) tracking implementation

## Commits (chronological)
- `ee4a696` — feat(mock-server): add MCP server compatibility and python-gvm integration test
- `1ce5f5e` — fix(mock-server): accept name fallback for note/override and add mcp integration spec
- `c245623` — ci: add python-gvm integration test job
- `269b97b` — fix: address all code review findings (17 items)
- `c074317` — ci: cross-platform release builds and nightly pipeline
- `8d02ff8` — fix(ci): resolve all CI failures
- `ee1b452` — docs: add README, implementation status page, and update dependabot config
- `6096d26` — fix(ci): update deny.toml to cargo-deny v2 schema
- `5ac3316` — ci(deps): bump the actions group with 5 updates (PR #11)
- `9b34f8b` — build(deps): bump russh from 0.48.2 to 0.57.1 (PR #14)
- `0f8aaa3` — fix(ci): remove remaining deprecated cargo-deny v2 fields
- `20995b6` — fix(ci): resolve cargo-deny failures (advisories + wildcards)
- `3605404` — fix(ci): add GPL-3.0-or-later to allowed licenses
- `2e1fc06` — fix(ci): use GITHUB_OUTPUT for nightly release date
- `d500f56` — spec: add openvas-mcp-server mock server integration test plan

## Rationale / Notes
- python-gvm two-connection flow is critical: connection 1 probes version, connection 2 authenticates. Mock server handles this via per-session auth state.
- Codex struggles with cargo-deny schema changes and rustfmt nightly options — these required manual iteration.
- Static musl builds are ideal for CI consumption by downstream projects (no shared lib deps).
- cargo-deny v2 removed many fields (`unlicensed`, `copyleft`, `vulnerability`, `unmaintained`, `yanked`, `notice`) — the migration took 3 commits to get right.

### Coverage Push (Session 2)
- Installed `cargo-llvm-cov` for per-file line coverage reporting
- Identified files below 80%: `history.rs` (61.7%), `version.rs` (70%), `xml_command.rs` (71.2%), `builder.rs` (76.9%), `request.rs` (0%)
- Codex created targeted tests: `builder_coverage.rs`, `store_extended.rs`, extended inline tests
- Coverage: **88.2% → 92.2%**, 249 tests (up from 221), all files ≥80%

### gvm-connection Implementation (Session 2, Codex-delegated)
- `GvmConnection` async trait (`connection.rs`) using `async-trait`
- `ConnectionError` enum (`error.rs`): NotConnected, AlreadyConnected, ConnectFailed, SendFailed, ReadFailed, Timeout, SocketNotFound
- `UnixSocketConnection` (`unix.rs`): connect/disconnect/send/read over `tokio::net::UnixStream`, uses `gvm_protocol::XmlReader` for frame detection
- `UnixSocketConfig`: path (default `/run/gvmd/gvmd.sock`), timeout (60s), read_buffer_size (64KB)
- Feature-gated: `unix` (default), `tls`/`ssh` stubs prepared
- Integration tests (`tests/unix_connection.rs`): connect+get_version, auth+create_target, reconnect flow, error paths
- Sandbox fix: mock server `unix_socket_auto()` → `cwd/.sock/` (socket path length limit), `create_dir_all` for parent dir
- All mock-server integration tests gated behind `#![cfg(feature = "unix-socket-tests")]`
- `async-trait` added as workspace dependency

### openvas-mcp-server PR (Session 2)
- Moved `spec/mcp-server-mock-integration.md` from rust-gvm to `docs/mock-server-integration.md` in openvas-mcp-server repo
- PR: [clawosiris/openvas-mcp-server#32](https://github.com/clawosiris/openvas-mcp-server/pull/32)
- Issue [#31](https://github.com/clawosiris/openvas-mcp-server/issues/31) updated — PR closes issue on merge

### Documentation Updates (Session 2)
- README.md: added gvm-connection section with usage example and transport status table; updated test count
- docs/STATUS.md: full gvm-connection trait/config/error/test tables; updated coverage stats to 92.2% / 255+ tests

## Commits (chronological, all sessions)
- `ee4a696` — feat(mock-server): add MCP server compatibility and python-gvm integration test
- `1ce5f5e` — fix(mock-server): accept name fallback for note/override and add mcp integration spec
- `c245623` — ci: add python-gvm integration test job
- `269b97b` — fix: address all code review findings (17 items)
- `c074317` — ci: cross-platform release builds and nightly pipeline
- `8d02ff8` — fix(ci): resolve all CI failures
- `ee1b452` — docs: add README, implementation status page, and update dependabot config
- `6096d26` — fix(ci): update deny.toml to cargo-deny v2 schema
- `5ac3316` — ci(deps): bump the actions group with 5 updates (PR #11)
- `9b34f8b` — build(deps): bump russh from 0.48.2 to 0.57.1 (PR #14)
- `0f8aaa3` — fix(ci): remove remaining deprecated cargo-deny v2 fields
- `20995b6` — fix(ci): resolve cargo-deny failures (advisories + wildcards)
- `3605404` — fix(ci): add GPL-3.0-or-later to allowed licenses
- `2e1fc06` — fix(ci): use GITHUB_OUTPUT for nightly release date
- `d500f56` — spec: add openvas-mcp-server mock server integration test plan
- `2334ab0` — journal: add 2026-03-15 session notes
- `0822bec` — test: add coverage tests for history, version, xml_command, builder, request
- `9680f39` — feat(connection): implement Unix socket transport with mock server integration tests

### gvm-gmp Implementation (Session 3, Codex-delegated)
- Full typed GMP command builder crate: 29 command modules, 23 GMP enums, `EntityId`/`GmpVersion` types
- `types.rs`: `EntityId` (validated identifiers), `GmpVersion(u16, u16)`
- `enums.rs`: macro-generated enums with `as_gmp_str()` and `FromStr` for all GMP wire values
- `common.rs`: shared helpers (`bool_str`, `add_text_element`, `add_filter_attrs`, `add_preferences`, etc.)
- Command modules: alerts, authentication, credentials, filters, groups, hosts, notes, nvts, overrides, permissions, port_lists, report_formats, reports, resource_names, results, roles, scan_configs, scanners, schedules, system, tags, targets, tasks, tickets, tls_certificates, trashcan, users, version
- Each module: create/clone/get/get_list/modify/delete + opts structs with `Default`
- 80 inline unit tests verifying XML output

### gvm-client Implementation (Session 3, Codex-delegated)
- `GmpClient<C>` — async connect/send/call/disconnect over any `GvmConnection`
- `GmpVersioned<C>` — version-negotiating enum (V224–VNext) with `inner()`/`inner_mut()` helpers
- `GvmError` type: Server, Connection (preserves `ConnectionError` source), XmlParse, UnsupportedVersion, Timeout, InvalidState
- Version parsing from GMP `get_version` response XML
- `map_supported_version()`: 22.4–22.7 mapped, 22.8+ → Next, <22.4 rejected
- 7 unit tests (version parsing/negotiation)

### Comprehensive Test Suite (Session 3, Codex-delegated)
- 31 new external test files in `crates/gvm-gmp/tests/`
- `test_enums.rs`: 347 exhaustive tests — every variant of every enum tested for `as_gmp_str()` + `FromStr` + invalid string
- Per-module command tests (test_alerts.rs through test_version.rs): basic, with-optionals, get/modify/delete/clone for each resource type
- `test_types.rs`: EntityId validation (valid UUID, empty, whitespace, special chars, Display, Hash, FromStr)
- Client integration tests expanded: version negotiation across 4 GMP versions, unsupported version error, full CRUD lifecycle (create_target → create_task → start → get → stop → delete), send-after-disconnect error
- Total test count: 218 → **623 tests**

### Rustdoc Coverage (Session 3, Codex-delegated)
- 67 files changed, all 531 `missing_docs` warnings eliminated
- Every public module, struct, field, function, enum, variant documented
- Enum macro updated: `#[doc = concat!(...)]` on enum type + `#[doc = ...]` on each variant
- Module-level `//! ...` doc comments added to all command modules

### Code Review (Session 3, Claude Code)
- 14 findings across 7 categories (2 high, 4 medium, 8 low)
- High: O(n²) re-parsing in `XmlReader::check_complete`, silent XML error discard in `UnixSocketConnection::read`
- Medium: `Response` re-parses XML 3× per `raise_for_status()`, `GvmError::Connection(String)` loses error source, `GmpVersioned` 5× match boilerplate, duplicate `ProtocolError`/`GvmError` hierarchy
- Low: `add_text_element` drops `Some("")`, `EntityId::new` allocates before validating, missing `#[must_use]`, test coverage gaps

### Review Fixes (Session 3, Codex-delegated)
- `XmlReader`: incremental parsing — `depth`, `seen_start`, `resume_offset` as struct fields; `Reader::from_reader` with `buffer_position()` tracking; `check_end_names = false` + `allow_unmatched_ends = true` for cross-chunk tag handling
- `UnixSocketConnection::read`: `feed()` errors propagated as `ConnectionError::ReadFailed(InvalidData)` instead of `let _ =`
- `Response`: `ParsedHeader` struct cached behind `OnceLock`; single parse for `status_code`/`status_text`/`root_element`/`id`; custom `Clone` impl preserves cache
- `GvmError::Connection`: now holds `#[source] ConnectionError` (was `String`); removed unused `Io` variant
- `GmpVersioned`: `inner()`/`inner_mut()` helpers; version wrapper tuple fields made private
- `Response`: `#[must_use]` added to `root_element_name()` and `id()`

### SBOM Generation (Session 3)
- Added `sbom` job to both release and nightly workflows
- Uses `cargo-cyclonedx` to generate CycloneDX SBOMs (JSON + XML) for all workspace crates
- Per-crate SBOMs collected, packaged as `rust-gvm-sbom.tar.gz` with SHA256 checksum
- Attached to GitHub releases alongside platform binaries

### License Change (Session 3)
- Changed from GPL-3.0-or-later to AGPL-3.0-or-later
- LICENSE file replaced with AGPL-3.0 full text
- SPDX headers added to all 113 `.rs` files:
  `// SPDX-License-Identifier: AGPL-3.0-or-later`
  `// SPDX-FileCopyrightText: 2026 Greenbone AG`
- `Cargo.toml` license field, `deny.toml` allowed licenses, README updated

### Clippy / CI Fixes (Session 3, multiple iterations)
- Added `#[must_use]` to all 156 command builder functions
- `#![allow(clippy::needless_pass_by_value)]` at `gvm-gmp` crate level (opts by-value is intentional)
- Added `# Errors` doc section to `EntityId::new`
- Test crate allows: `missing_docs`, `unwrap_used`, `missing_panics_doc`, `must_use_candidate`, `needless_pass_by_value`
- Removed global `RUSTFLAGS=-Dwarnings` from ci.yml (was preventing test crate `#![allow]` overrides)

## Commits (Session 3, chronological)
- `cc6c431` — docs: update README, STATUS, and journal for gvm-connection and coverage work
- `6617c10` — feat(gmp): implement typed GMP command builder crate
- `48147dd` — feat(client): implement high-level async GMP client with version negotiation
- `78de533` — test(gmp): add comprehensive command, enum, and client integration coverage
- `abb184a` — docs(gmp): add comprehensive rustdoc coverage for all public API items
- `a658c51` — docs: update README and STATUS for gvm-gmp, gvm-client, and test coverage
- `a9f108e` — ci: add CycloneDX SBOM generation to release and nightly workflows
- `d249bb0` — fix: address code review findings (XmlReader O(n²), error propagation, Response caching, error source chain, GmpVersioned cleanup)
- `a4c9c32` — license: change to AGPL-3.0-or-later with Greenbone AG copyright
- `ea88aa6` — fix(clippy): add #[must_use] to all command builders, suppress test-only lints
- `5d2ae57` — fix(ci): remove global RUSTFLAGS=-Dwarnings to allow test crate #![allow] overrides

### Session 4 — quick-xml migration + SSH transport
- Upgraded workspace `quick-xml` dependency from `0.37` to `0.39`.
- Migrated text extraction calls from `BytesText::unescape()` to `BytesText::xml_content()` in:
  - `crates/gvm-protocol/src/response.rs`
  - `crates/gvm-mock-server/src/command_parser.rs`
- Implemented `gvm-connection` SSH transport (`ssh` feature) in `crates/gvm-connection/src/ssh.rs`:
  - `SshConfig` with builder-style helpers (`with_port`, `with_remote_socket`, `with_timeout`)
  - `SshAuth` enum (`Password`, `PrivateKey`, `Agent`)
  - `SshConnection` implementing `GvmConnection` (`connect`, `disconnect`, `send`, `read`, `is_connected`)
  - Remote gvmd socket tunnel via `channel_open_direct_streamlocal`
  - Timeout handling across connect/auth/read/write paths
- Exposed SSH API from crate root (`pub use ssh::{SshAuth, SshConfig, SshConnection};`).
- Added SSH tests in `crates/gvm-connection/tests/ssh_connection.rs`.
- Hardened socket-based integration tests to skip gracefully on sandbox `PermissionDenied` (instead of failing) across mock-server/connection test suites.

### Validation (Session 4)
- `cargo test --workspace` ✅
- `cargo test --workspace --all-features` ✅
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` ✅
- `cargo doc --workspace --all-features --no-deps` ✅

### Commits (Session 4)
- `593b259` — chore: add CODEX_*.md to .gitignore and clean up task specs
- `d6857a4` — fix(ci): remove invalid --output-cdx flag from cargo-cyclonedx commands
- `ca07e1b` — feat(connection): add ssh transport and migrate to quick-xml 0.39
- `b7bc9aa` — journal: record quick-xml 0.39 migration and ssh transport session
- `7c5e00e` — docs: add client crate usage examples and update transport status

### Session 5 — SSH mock server, E2E tests, specs, security, release

#### Mock Server SSH Listener (Codex-delegated)
- New `crates/gvm-mock-server/src/ssh_listener.rs`:
  - `russh::server::Handler` with ephemeral Ed25519 host key
  - Password auth checking against `ResourceStore` credentials (Stateful mode) or accept-all (other modes)
  - `direct-streamlocal@openssh.com` channel handling → routes to `SessionHandler` pipeline
  - `ResourceStore::credentials_match()` added for SSH auth
- Builder: `.ssh("127.0.0.1:0")` transport variant
- Server: `ssh_addr()` / `ssh_port()` accessors
- Feature-gated behind `ssh` in `gvm-mock-server/Cargo.toml`

#### SSH E2E Tests
- `crates/gvm-connection/tests/ssh_e2e.rs` — 4 tests:
  - `ssh_connect_and_get_version` — SshConnection → mock SSH server → get_version
  - `ssh_authenticate_and_crud` — full flow: auth → create_target → get_targets → delete
  - `ssh_not_connected_errors` — send/read fail before connecting
  - `ssh_reconnect_flow` — connect → disconnect → reconnect → auth → commands

#### Spec Updates
- `spec/openspec.md`:
  - License: GPL → AGPL-3.0-or-later with SPDX headers
  - `SshConfig`/`SshAuth` API updated to actual implementation
  - `ConnectionError` and `GvmError` enums updated to match code
  - New §7 CI/CD & Build Infrastructure (workflows, SBOM, Dependabot, Makefile)
  - Test strategy expanded with actual test counts (630+)
  - Phase status markers (✅/⬜) added
- `spec/mock-server-openspec.md`:
  - New §3.3 Transport Listeners (SSH listener details)
  - `russh` added to dependencies
  - Phase status markers and Phase 4 additions documented

#### Documentation
- README: client crate usage examples (GmpClient, GmpVersioned, SSH transport)
- README: updated transport table (SSH implemented)
- STATUS.md: expanded gvm-client API tables, SshConfig reference, test counts

#### Releases
- v0.1.0: tagged, release workflow triggered, SBOM fix applied, re-tagged
- v0.1.1: tagged with SSH transport + mock server SSH listener

#### openvas-mcp-server Integration Tests
- PR [#33](https://github.com/clawosiris/openvas-mcp-server/pull/33): 14 tests across 5 files
- Commit `058324d` on branch `feat/mock-server-integration-tests`

#### Security Hardening
- Enabled Dependabot vulnerability alerts
- Enabled Dependabot automated security fixes
- New `.github/workflows/security.yml`:
  - `cargo-audit` — Rust advisory database scan (weekly + push/PR)
  - `cargo-machete` — unused dependency detection
  - OpenSSF Scorecard — supply chain security assessment with SARIF upload
- Branch protection deferred: requires public repo or GitHub Pro

#### Branching
- Created `dev` branch (pushed to origin)
- Branch protection deferred: private repo on free plan

### Validation (Session 5)
- `cargo test --workspace --all-features` ✅ (all tests including SSH E2E)
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` ✅
- CI run `23121041798`: all 10 jobs green

### Commits (Session 5)
- `0b81e74` — feat(mock-server): add SSH listener and E2E tests
- `40562ff` — docs(spec): update openspec and mock-server spec to reflect implementation
- `4c0f0ac` — ci: add security workflow (cargo-audit, cargo-machete, OpenSSF Scorecard)
- v0.1.1 tag pushed

### GitHub Issues Status
- #1, #2, #3, #5, #6 — closed (completed)
- #8 — closed (not planned)
- #4 — open (large-response streaming, enhancement)
- #7 — open (TLS transport, enhancement)
- #9 — open (record-and-replay, enhancement)

## Open Work
- TLS transport in gvm-connection (issue #7)
- Large-response streaming strategy (issue #4)
- Record-and-replay support roadmap (issue #9)
- Branch protection — enable when repo goes public or on Pro plan
- Secret scanning / push protection — enable when repo goes public
