# 2026-05-06 — Mainline Moving Again, Queue Reduced to Three Real Decisions

## Mainline & Merged Work

### `main` finally moved again after the 2026-05-04 journal baseline
- Nine default-branch commits landed after the last journal entry
- The important merges were PR #109 (`fix(ci): restore release.yml with SBOM, Docker, binaries`), PR #121 (`ci(deps): bump the actions group across 1 directory with 4 updates`), and PR #126 (`build(deps): bump rand from 0.8.5 to 0.8.6`) on 2026-05-04
- On 2026-05-05, PR #145 (`ci: run security workflow on docs-only pull requests`) merged and removed a real automation blind spot for journal and docs-only branches
- Journal backlog PRs #135, #136, #137, #138, and #139 also merged on 2026-05-05, so `main` now contains the journal history through 2026-05-04

## Pull Request Queue

### The backlog is smaller and easier to reason about now
- PR #144 (`feat(client): complete typed scan config and scanner helpers`) opened on 2026-05-05 and is the highest-value active lane; its full CI and Security stack is green
- PR #141 (`build(deps): bump the cargo group across 1 directory with 7 updates`) is still open and fully green
- Journal PR #142 (`docs(journal): add 2026-05-05 journal entry`) is also open and green, now with Security running correctly on docs-only work because of PR #145
- Two superseded dependency PRs (#131 and #140) were closed on 2026-05-04, and older single-purpose dependency PRs #114, #125, and #128 were closed on 2026-05-05 as the queue consolidated

## Issues
- New issue #143 (`feat(client): add missing typed scan-config and scanner helpers`) opened on 2026-05-05
- That issue already has a matching implementation lane in PR #144, so the repo's newest issue is not blocked by missing code work
- No issues were closed after the last journal entry

## CI Health
- `main` has fresh successful `CI`, `Security`, and repeated `OpenSSF Scorecard` push runs on 2026-05-05
- The docs-only Security gap is fixed: journal PR #142 now gets a successful `Security` run instead of silently skipping that coverage
- Mainline Dependabot noise did not disappear entirely: one 2026-05-04 GitHub Actions dynamic update run and one same-day `Security` push run failed before newer green signals arrived
- The repo is in a much healthier state than the 2026-05-04 entry suggested; the main risk now is review throughput, not broken automation
