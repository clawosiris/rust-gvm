# 2026-07-28 — Two GMP Corrections Advanced and Sync Semantics Need Repair

## GMP Compatibility Fixes
- PR [#412](https://github.com/clawosiris/rust-gvm/pull/412) now scopes typed scan-config getters to `usage_type="scan"`, preventing policy resources from leaking into scan-config results while preserving the generic config APIs.
- PR [#413](https://github.com/clawosiris/rust-gvm/pull/413) now emits gvmd's nested subject and resource reference shape for permission create and modify requests, with stateful mock-server and real Unix-transport regression coverage.
- Both fixes are mergeable and green across required CI, integration, coverage, and security checks; each still needs review before merging into `devel`.

## Newly Exposed Sync Contract Gap
- Community E2E expansion opened issue [#414](https://github.com/clawosiris/rust-gvm/issues/414) after the public sync helper sent `<sync_config config_id="..."/>`, which current gvmd rejects because the GMP schema defines a global parameterless `<sync_config/>` request.
- The correction must align the typed facade with the command's global semantics while deliberately handling compatibility for the existing ID-taking helper, and add exact wire and typed-response regressions.
